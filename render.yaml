services:
  - type: web
    name: offerbuddy-ai
    env: node
    region: oregon # Render's default region, you can remove or change
    buildCommand: npm install && npm run build
    startCommand: npm run start
    envVars:
      - key: NODE_ENV
        value: production
      - key: GEMINI_API_KEY
        sync: false
